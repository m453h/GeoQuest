<!DOCTYPE html>

<title>Edit FactType</title>

{% block body %}
    <h1>Edit FactType</h1>

    {{ include('configuration/fact_type/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_configuration_fact_type_index') }}">back to list</a>

    {{ include('configuration/fact_type/_delete_form.html.twig') }}
{% endblock %}

<!DOCTYPE html>

<title>FactType</title>

{% block body %}
    <h1>FactType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fact_type.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ fact_type.description }}</td>
            </tr>
            <tr>
                <th>ApiField</th>
                <td>{{ fact_type.apiField }}</td>
            </tr>
            <tr>
                <th>QuestionPrompt</th>
                <td>{{ fact_type.questionPrompt }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_configuration_fact_type_index') }}">back to list</a>

    <a href="{{ path('app_configuration_fact_type_edit', {'id': fact_type.id}) }}">edit</a>

    {{ include('configuration/fact_type/_delete_form.html.twig') }}
{% endblock %}
